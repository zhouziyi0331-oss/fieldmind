# FieldMind 架构升级方案 - 设计文档

> 基于《2026年企业AI工作流重构研究报告》进行架构升级
> 
> **设计原则**：
> - 分模块、扎实推进，不快速、不假设、不反复
> - 面向个人和小团队使用场景
> - 为未来多人协作预留扩展接口
> - macOS原生应用
> - 先后端 → 再前端 → 最后解决显示bug

---

## 模块1：权限系统（Permission System）✅ 已确认

### 设计目的
为未来多人协作预留接口，当前支持单用户使用。

### 核心组件

#### 1. 数据表
- **Role**（角色表）
  - 支持系统级角色和项目级角色
  - 字段：id, name, display_name, description, is_system, is_default, project_id, priority, is_active

- **Permission**（权限表）
  - 定义资源类型 + 操作
  - 字段：id, code, display_name, resource_type, action, scope, is_system, is_active

- **ProjectMember**（项目成员表）
  - 记录用户在项目中的成员关系
  - 字段：id, project_id, user_id, role_id, is_active, joined_at, invited_by

- **ResourceOwnership**（资源所有权表）
  - 记录资源的创建者和所有者
  - 字段：id, resource_type, resource_id, owner_id, project_id, created_at

#### 2. 枚举类型
- **PermissionAction**: VIEW, CREATE, EDIT, DELETE, SHARE, EXPORT, MANAGE
- **ResourceType**: PROJECT, DOCUMENT, PHOTO, TABLE, WORKFLOW, SKILL, KNOWLEDGE_GRAPH, REPORT, CHAT_SESSION, TIMELINE

#### 3. 关联表
- **role_permissions**: 角色-权限多对多关联
- **user_roles**: 用户-角色多对多关联（支持项目级）

### 设计特点
- 基于RBAC（基于角色的权限管理）
- 支持项目级和系统级角色
- 支持细粒度的权限控制（scope字段）
- 为多人协作预留完整扩展接口

### 实现文件
- `/Users/alwan/FieldMind/backend/src/app/models/permission.py`

---

## 模块2：审计日志系统（Audit Log System）✅ 已确认

### 设计目的
记录所有关键操作，实现操作可追溯，用于问题排查和复盘。

### 核心需求

#### 需要记录的操作
1. **用户操作**
   - ✅ 创建（CREATE）
   - ✅ 修改（UPDATE）
   - ✅ 删除（DELETE）
   - ❌ 查看（不记录）

2. **AI操作**
   - ✅ AI生成的内容
   - ✅ AI的建议
   - ✅ AI做的删改

3. **权限操作**
   - ❌ 角色分配（不记录，当前单用户场景不需要）

#### 不需要记录的
- ❌ 自动任务的操作
- ❌ 查看操作
- ❌ 系统级操作

### 数据表设计

#### AuditLog（审计日志表）
```python
字段：
- id: 主键
- timestamp: 操作时间
- actor_type: 操作者类型（USER/AI/SYSTEM）
- actor_id: 操作者ID
- action: 操作类型（CREATE/UPDATE/DELETE）
- resource_type: 资源类型
- resource_id: 资源ID
- resource_name: 资源名称（便于查看）
- changes: 变更内容（JSON，记录前后对比）
- ai_model: AI模型名称（如果是AI操作）
- ai_prompt: AI提示词（如果是AI操作）
- result: 操作结果（SUCCESS/FAILURE）
- error_message: 错误信息（如果失败）
- metadata: 其他元数据（JSON）
```

### 日志策略
- **保留时间**：15天（节省存储空间）
- **清理机制**：定时任务自动清理15天前的日志
- **存储方式**：数据库存储
- **查询功能**：不需要复杂查询，只需基础的列表展示

### 实现要点
- 通过装饰器或中间件自动记录
- 异步写入，不阻塞主流程
- 失败不影响业务操作

---

## 模块3：工作流引擎（Workflow Engine）✅ 已确认

### 设计目的
自动化执行完整的数据处理流程，并实时展示处理进度。

### 核心理念
**AI对话是最后一步，而不是第一步**。必须先有数据、先有分析、先有知识体系，AI对话才有意义。

### 完整工作流（8个步骤）

```
1. 上传资料（图片/视频/音频）
   ↓
2. 关键词提取
   ↓
3. 知识脉络生成（Vein）
   ↓
4. 编年史生成（Chronicle）
   ↓
5. 时间线生成（Timeline）
   ↓
6. 知识图谱生成（Knowledge Graph）
   ↓
7. 业态分析报告生成（Report 1/2/3）
   ↓
8. AI对话准备就绪
```

### 核心功能要求

#### 1. 自动化执行
- 用户上传资料后，自动触发完整流程
- 每个步骤完成后，自动触发下一步
- 无需人工干预（除非异常）

#### 2. 实时状态展示
- ✅ 显示当前执行到哪一步
- ✅ 显示每一步的完成状态（待处理/处理中/已完成/失败）
- ✅ 显示预计剩余时间
- ✅ **流式更新**：生成一个就立即更新UI，不等全部完成

#### 3. 条件分支
- 如果某一步失败，可以重试或跳过
- 根据资料类型，某些步骤可能不适用（如：纯文本不需要OCR）

#### 4. 可视化展示
- 显示流程图：当前在哪一步
- 显示进度条：整体完成百分比
- 显示每一步的详细信息

### 数据表设计

#### WorkflowExecution（工作流执行表）
```python
字段：
- id: 主键
- project_id: 所属项目
- trigger_type: 触发类型（MANUAL/AUTO）
- status: 总体状态（PENDING/RUNNING/COMPLETED/FAILED）
- current_step: 当前步骤索引
- total_steps: 总步骤数
- progress_percentage: 完成百分比
- started_at: 开始时间
- completed_at: 完成时间
- estimated_completion: 预计完成时间
- error_message: 错误信息（如果失败）
```

#### WorkflowStep（工作流步骤表）
```python
字段：
- id: 主键
- execution_id: 所属执行
- step_name: 步骤名称（KEYWORD_EXTRACTION/VEIN_GENERATION等）
- step_order: 步骤顺序
- status: 步骤状态（PENDING/RUNNING/COMPLETED/FAILED/SKIPPED）
- input_data: 输入数据（JSON）
- output_data: 输出数据（JSON）
- started_at: 开始时间
- completed_at: 完成时间
- duration_seconds: 执行时长
- error_message: 错误信息
- retry_count: 重试次数
```

#### WorkflowTemplate（工作流模板表）
```python
字段：
- id: 主键
- name: 模板名称
- description: 模板描述
- steps_definition: 步骤定义（JSON）
- is_default: 是否为默认模板
- is_active: 是否启用
```

### 实现要点

#### 后端
1. **异步任务执行**（使用Celery或类似的任务队列）
2. **WebSocket推送**（实时推送进度更新）
3. **断点续传**（如果某一步失败，可以从失败处继续）
4. **并行执行**（某些步骤可以并行，如知识脉络和编年史可以同时生成）

#### 前端
1. **实时进度展示**（订阅WebSocket更新）
2. **流程可视化**（显示流程图）
3. **详细信息面板**（点击某一步查看详细信息）
4. **手动干预**（允许暂停、重试、跳过某一步）

### 与现有功能的关系
- 现有的 WorkflowPage 和 WorkflowService 需要重构
- 整合现有的各个独立功能（关键词提取、知识图谱生成等）
- 形成统一的自动化流程

---

## 模块4：数据追溯系统（Data Lineage）✅ 已确认

### 设计目的
实现"数据可信" - 清晰标记数据来源、版本历史和依赖关系，确保所有内容可追溯。

### 核心功能

#### 1. 来源标记
每个数据都必须标记来源：
- **用户上传**：原始文件（照片/视频/音频/文档）
- **AI生成**：哪个模型、什么时间生成的
- **提取得到**：从哪个文件的哪个片段提取的

#### 2. 完整的依赖链路（三层报告结构）

```
原始资料（照片/视频/音频/文档）
  ↓ 提取
关键词
  ↓ 生成
知识脉络（Vein）+ 编年史（Chronicle）+ 时间线（Timeline）
  ↓ 生成
知识图谱（Knowledge Graph）
  ↓ 分析
报告1：基础资料分析
  ↓ 基于报告1 + 人文书籍数据
报告2：人文视角分析
  ↓ 基于报告2 + 商业分析数据
报告3：商业视角分析（最终报告）
```

#### 3. 版本管理策略

**触发新版本的操作：**
- ✅ 用户上传新资料
- ✅ 用户主动修改内容
- ✅ 用户删除资料

**不触发新版本的操作：**
- ❌ AI自动重新生成（直接覆盖旧内容）
- ❌ 系统自动优化

**动态更新机制：**
- 用户上传新资料后，自动触发完整的工作流
- 所有依赖该资料的内容都会动态更新
- 形成完整的更新链路

#### 4. 血缘关系可视化

在知识图谱中展示：
- **向上追溯**：这个节点来自哪里（数据源）
- **向下追溯**：这个节点影响了什么（下游依赖）
- **横向关联**：同一来源的其他数据

示例：
```
用户点击报告3中的某个结论
  ↓ 显示
该结论来自报告2的XX分析
  ↓ 显示
报告2的XX分析来自报告1的YY数据
  ↓ 显示
报告1的YY数据来自知识图谱的ZZ节点
  ↓ 显示
ZZ节点来自关键词"产品"
  ↓ 显示
关键词"产品"提取自照片A.jpg的第3段文字
  ↓ 显示
照片A.jpg是用户在2026-08-25上传的
```

### 数据表设计

#### DataLineage（数据血缘表）
```python
字段：
- id: 主键
- source_type: 来源类型（USER_UPLOAD/AI_GENERATED/EXTRACTED）
- source_id: 来源ID（文件ID/AI任务ID等）
- source_detail: 来源详情（JSON，如：文件名、片段位置、AI模型等）

- target_type: 目标类型（KEYWORD/VEIN/GRAPH/REPORT等）
- target_id: 目标ID
- target_detail: 目标详情（JSON）

- relationship_type: 关系类型（EXTRACTED_FROM/GENERATED_FROM/BASED_ON）
- created_at: 创建时间
- metadata: 其他元数据（JSON）
```

#### DataVersion（数据版本表）
```python
字段：
- id: 主键
- resource_type: 资源类型
- resource_id: 资源ID
- version_number: 版本号
- content: 内容快照（JSON或TEXT）
- change_type: 变更类型（CREATE/UPDATE/DELETE）
- changed_by: 变更人（USER_ID或AI）
- change_reason: 变更原因
- created_at: 创建时间
```

#### SourceFile（源文件表）
```python
字段：
- id: 主键
- project_id: 所属项目
- file_name: 文件名
- file_type: 文件类型（PHOTO/VIDEO/AUDIO/DOCUMENT）
- file_path: 文件路径
- uploaded_by: 上传者
- uploaded_at: 上传时间
- file_hash: 文件哈希（用于去重）
- extracted_content: 提取的内容（JSON）
- is_active: 是否有效
```

### 实现要点

#### 后端
1. **自动记录血缘关系**
   - 每次生成内容时，自动创建血缘记录
   - 通过装饰器或AOP方式自动记录

2. **版本快照**
   - 用户修改时，自动保存旧版本
   - 支持版本对比和回滚

3. **依赖更新传播**
   - 当源数据更新时，自动标记下游数据为"待更新"
   - 触发工作流重新生成

#### 前端
1. **血缘关系图**
   - 在知识图谱中集成血缘关系展示
   - 支持双向追溯（向上和向下）

2. **来源标签**
   - 每个内容都显示来源标签
   - 点击标签可查看详细来源

3. **版本历史**
   - 显示版本列表
   - 支持版本对比和恢复

---

## 模块5：指标体系（Metrics System）✅ 已确认

### 设计目的
**对外展示价值** - 通过可视化指标，让用户直观看到系统做了什么工作，创造了多少价值。

### 核心理念
不是为了内部管理，而是为了**对外展示和价值证明**。

### 两大类指标

#### 1. 数据指标（量化成果）

##### 准确率指标
- **关键词提取准确率**
  - AI提取的关键词质量
  - 用户认可的关键词比例
  
- **知识图谱质量评分**
  - 节点关联的准确性
  - 知识脉络的完整性

##### 完成量指标
- **文件处理量**
  - 处理的照片数量
  - 处理的视频数量
  - 处理的音频数量
  - 处理的文档数量

- **知识生成量**
  - 提取的关键词数量
  - 生成的知识节点数量
  - 建立的关联关系数量
  - 生成的报告数量

- **任务完成率**
  - 成功完成的工作流数量
  - 失败的工作流数量
  - 平均完成时长

#### 2. 价值指标（工作量化）

**目标：可视化解释系统干了哪些工作，相当于多少人力**

##### 工作量换算
```
系统完成的工作 → 换算成人工需要的时间/成本

示例：
- 处理 1 份文档（OCR + 分析）      ≈ 30分钟人工
- 提取 100 个关键词                ≈ 2小时人工
- 生成 1 个知识图谱                ≈ 4小时人工
- 生成 1 份深度报告                ≈ 8小时人工
```

##### 价值展示维度
1. **时间节省**
   - 节省了多少小时的人工分析时间
   - 效率提升了多少倍

2. **成本节省**（可选）
   - 按人工成本换算，节省了多少费用
   - 投资回报率（ROI）

3. **工作类型分解**
   - 完成了哪些类型的工作
   - 每种工作的占比

### 展示形式

#### 仪表板（Dashboard）
在系统首页或专门的指标页面展示：

```
┌─────────────────────────────────────────┐
│  FieldMind 工作总览                      │
├─────────────────────────────────────────┤
│  📊 本项目已完成                         │
│                                         │
│  📄 处理文档      150 份                │
│  🔑 提取关键词    1,200 个              │
│  🕸️ 知识节点      800 个                │
│  📈 生成报告      3 份                  │
│                                         │
│  ⏱️ 工作流耗时    平均 15 分钟          │
│  ✅ 完成率       95%                    │
│  🎯 准确率       92%                    │
├─────────────────────────────────────────┤
│  💡 价值等效                            │
│                                         │
│  节省人工时间：  120 小时               │
│  效率提升：      10 倍                  │
│  等效工作量：    15 人/天               │
└─────────────────────────────────────────┘
```

#### 详细报告
支持生成详细的指标报告（PDF/HTML），用于对外展示：
- 项目周期内的所有工作
- 每个阶段的成果
- 价值量化说明

### 数据表设计

#### MetricsSnapshot（指标快照表）
```python
字段：
- id: 主键
- project_id: 所属项目
- snapshot_date: 快照日期
- metrics_data: 指标数据（JSON）
  {
    "files_processed": 150,
    "keywords_extracted": 1200,
    "knowledge_nodes": 800,
    "reports_generated": 3,
    "accuracy_rate": 0.92,
    "completion_rate": 0.95,
    "avg_workflow_duration": 900,  // 秒
    "total_time_saved": 7200,      // 秒
    "efficiency_multiplier": 10
  }
- created_at: 创建时间
```

#### WorkloadCalculation（工作量计算表）
```python
字段：
- id: 主键
- task_type: 任务类型（DOCUMENT_PROCESSING/KEYWORD_EXTRACTION等）
- task_count: 任务数量
- manual_hours_per_task: 每个任务需要的人工小时数
- total_manual_hours: 总人工小时数
- calculated_at: 计算时间
```

#### ValueExhibition（价值展示配置表）
```python
字段：
- id: 主键
- task_type: 任务类型
- display_name: 展示名称
- unit_value: 单位价值（时间/成本）
- calculation_formula: 计算公式
- is_active: 是否启用
```

### 实现要点

#### 后端
1. **实时统计**
   - 每完成一个任务，实时更新指标
   - 使用Redis缓存当前指标，避免频繁查询数据库

2. **定期快照**
   - 每天生成一次指标快照
   - 用于历史趋势分析

3. **工作量换算**
   - 预设各类任务的人工工作量标准
   - 支持自定义调整换算比例

#### 前端
1. **实时展示**
   - 在Dashboard显示最新指标
   - 使用动画效果增强视觉效果

2. **趋势图表**
   - 显示指标随时间的变化
   - 支持按日/周/月查看

3. **导出报告**
   - 支持导出PDF/HTML格式的价值报告
   - 用于对外展示

---

## 模块6：工作舱（Workbench）✅ 已确认

### 设计目的
提供统一的项目工作界面，降低信息摩擦和系统摩擦。

### 核心理念
**一个项目 = 一个独立工作舱**

所有工作都围绕项目展开，工作舱是项目的完整工作界面。

### 工作舱结构

```
项目工作舱（Project Workbench）
│
├── 📁 资料管理（Materials）
│   ├── 文档（Documents）
│   ├── 照片（Photos）
│   ├── 表格（Tables）
│   ├── 视频（Videos）
│   └── 音频（Audios）
│
├── 🧠 知识处理（Knowledge Processing）
│   ├── 关键词（Keywords）
│   ├── 知识脉络（Vein）
│   ├── 编年史（Chronicle）
│   ├── 时间线（Timeline）
│   └── 知识图谱（Knowledge Graph）
│
├── 📊 分析报告（Analysis Reports）
│   ├── 报告1：基础资料分析
│   ├── 报告2：人文视角分析
│   └── 报告3：商业视角分析
│
├── 💬 AI对话（AI Chat）
│   └── 基于项目完整知识体系的对话
│
├── ⚙️ 工作流状态（Workflow Status）
│   ├── 当前处理进度
│   ├── 待处理任务
│   └── 已完成任务
│
└── 📈 指标展示（Metrics Dashboard）
    ├── 数据指标
    ├── 价值量化
    └── 工作总览
```

### 关键特点

#### 1. 项目制思维
- **所有思考围绕项目**，不是围绕单独的任务或文档
- 一个项目可能持续数周甚至数月
- 项目是知识积累的最小完整单元

#### 2. 统一工作界面
- **不需要在不同页面跳转**
- 一个项目的所有内容都在工作舱内
- 左侧导航显示项目内的各个模块

#### 3. 场景作为子任务
项目可以包含多种分析场景：
- **研究分析**（学术研究）
- **商业分析**（市场调研、竞品分析）
- **社会分析**（如费孝通式的田野调查、人文观察）

这些场景都是项目的不同维度，不是独立的工作舱。

#### 4. 知识的完整性
- 资料 → 知识 → 分析 → 对话，形成完整闭环
- AI对话基于项目的完整知识体系
- 不是碎片化的单点工具

### 与现有架构的关系

#### 需要重构的部分
- **当前**：独立的页面（DocumentPage, PhotoPage, GraphPage等）
- **未来**：项目工作舱内的模块（Documents Module, Photos Module等）

#### 主导航的变化
```
当前导航：
- 项目列表
- 文档管理
- 照片管理
- 表格管理
- 关键词
- 知识图谱
- ...

未来导航：
- 项目列表（选择项目）
  ↓ 进入项目工作舱
- 资料管理 ◄─┐
- 知识处理   │ 项目工作舱内的模块
- 分析报告   │
- AI对话     │
- 工作流     │
- 指标     ◄─┘
```

### 界面设计草图

```
┌─────────────────────────────────────────────────────────────┐
│ FieldMind                    [项目：费孝通田野调查]    [@user] │
├─────────────────────────────────────────────────────────────┤
│                 │                                           │
│  📁 资料管理     │  当前处理状态                              │
│  🧠 知识处理     │  ━━━━━━━━━━━━━━━━━━━━━━━━━━ 85%         │
│  📊 分析报告     │  正在生成知识图谱...                       │
│  💬 AI对话      │                                           │
│  ⚙️ 工作流      │  ┌─────────────────────────────────┐    │
│  📈 指标        │  │ 📊 项目指标                      │    │
│                 │  │  处理文档: 45 份                 │    │
│  ───────────    │  │  关键词: 580 个                  │    │
│                 │  │  知识节点: 320 个                │    │
│  📌 最近上传    │  │  完成度: 85%                     │    │
│  • 江村经济.pdf │  │  ⏱️ 节省时间: 60 小时             │    │
│  • 访谈录音.mp3 │  └─────────────────────────────────┘    │
│  • 田野照片.jpg │                                           │
│                 │  ┌─────────────────────────────────┐    │
│  🔔 待处理任务  │  │ 📄 最新生成                      │    │
│  • 生成报告3    │  │  ✓ 关键词: 乡土社会、差序格局     │    │
│                 │  │  ✓ 知识图谱: 85% 完成            │    │
│                 │  │  ⏳ 报告2: 生成中...             │    │
│                 │  └─────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 数据表设计

#### ProjectWorkbench（项目工作舱表）
```python
字段：
- id: 主键
- project_id: 所属项目
- workbench_config: 工作舱配置（JSON）
  {
    "layout": "default",
    "visible_modules": ["materials", "knowledge", "reports", "chat"],
    "module_order": [...],
    "dashboard_widgets": [...]
  }
- last_active_module: 最后活跃的模块
- created_at: 创建时间
- updated_at: 更新时间
```

#### WorkbenchActivity（工作舱活动记录）
```python
字段：
- id: 主键
- project_id: 所属项目
- user_id: 用户ID
- module_name: 模块名称
- action: 动作（VIEW/EDIT/CREATE等）
- duration_seconds: 停留时长
- timestamp: 时间戳
```

### 实现要点

#### 前端
1. **项目选择器**
   - 首页显示项目列表
   - 选择项目后进入工作舱

2. **工作舱布局**
   - 左侧：模块导航
   - 中间：主工作区
   - 右侧：状态面板（可折叠）

3. **模块化加载**
   - 按需加载各个模块
   - 保持模块间的状态同步

#### 后端
1. **项目上下文**
   - 所有API请求都带上 project_id
   - 确保数据隔离

2. **工作舱配置**
   - 支持自定义工作舱布局
   - 保存用户偏好设置

---

## 模块7：资产库（Asset Library）✅ 已确认

### 设计目的
把成功的项目经验沉淀为可复用的资产，方便在新项目中复用。

### 核心理念
**一切有价值的工作经验都应该沉淀为可复用的Skill**

### 资产类型

#### 1. 知识框架 Skill（已有）
FieldMind 已经有的 Skill 系统，包括：
- 社会学分析框架
- 社会分析框架
- 商业分析框架
- ...

这些都是可复用的知识框架，需要继续完善和扩展。

#### 2. 项目复盘 Skill（新增）
把每个项目的经验沉淀为 Skill：
- **场景定义**：项目适用的场景
- **流程配置**：成功的工作流配置
- **知识规则**：关键词提取规则、分析维度
- **复盘总结**：项目中的经验教训

#### 3. 思考模式 Skill（新增）
**核心创新点：从用户的工作痕迹中学习**

- 用户关注的问题
- 用户的对话模式
- 用户的思考路径
- 用户的分析角度

**目标**：AI 通过学习用户的工作痕迹，自动生成用户专属的思考模式 Skill。

用户可能自己不知道自己的思路规律，但 AI 可以：
- 分析用户的对话历史
- 识别用户的关注点
- 提取用户的思维模式
- 形成可复用的 Skill

### Skill 的动态更新

所有 Skill 都应该是**动态更新**的：
- 不是一次性创建
- 随着项目的进行不断优化
- 从用户的使用中学习

### 数据表设计

#### SkillAsset（Skill 资产表）
```python
字段：
- id: 主键
- skill_name: Skill 名称
- skill_type: Skill 类型
  - KNOWLEDGE_FRAMEWORK（知识框架）
  - PROJECT_REPLAY（项目复盘）
  - THINKING_PATTERN（思考模式）
- skill_content: Skill 内容（JSON）
  {
    "framework": {...},
    "rules": [...],
    "prompts": [...],
    "examples": [...]
  }
- source_project_id: 来源项目（如果是从项目中提取）
- usage_count: 使用次数
- success_rate: 成功率
- version: 版本号
- is_active: 是否启用
- created_at: 创建时间
- updated_at: 更新时间
- created_by: 创建者（USER/AI）
```

#### ThinkingPatternLearning（思考模式学习表）
```python
字段：
- id: 主键
- user_id: 用户ID
- project_id: 所属项目
- interaction_type: 交互类型（CHAT/EDIT/ANNOTATION等）
- content: 内容
- extracted_patterns: 提取的模式（JSON）
  {
    "focus_areas": ["社会结构", "权力关系"],
    "analysis_dimensions": ["历史", "现状", "未来"],
    "question_patterns": [...]
  }
- learned_at: 学习时间
```

#### SkillEvolution（Skill 进化记录表）
```python
字段：
- id: 主键
- skill_id: Skill ID
- version_from: 旧版本
- version_to: 新版本
- changes: 变更内容（JSON）
- improvement_reason: 改进原因
- triggered_by: 触发方式（USER_FEEDBACK/AUTO_LEARNING）
- created_at: 创建时间
```

### 实现要点

#### 后端
1. **Skill 自动提取**
   - 从项目完成后自动提取成功的配置
   - 从用户对话中提取思考模式

2. **Skill 推荐**
   - 新建项目时推荐相关 Skill
   - 基于项目类型和用户历史

3. **Skill 进化**
   - 根据使用效果自动优化
   - 接受用户反馈进行调整

#### 前端
1. **Skill 管理界面**
   - 浏览所有 Skill
   - 查看 Skill 的使用统计
   - 手动创建/编辑 Skill

2. **Skill 应用**
   - 创建项目时选择 Skill
   - 在工作舱中切换 Skill

3. **Skill 学习可视化**
   - 显示 AI 从用户行为中学到了什么
   - 让用户确认或调整学习结果

---

## 📝 7个模块设计总结

### ✅ 已确认的模块

| 模块 | 核心目的 | 主要功能 |
|------|---------|---------|
| 1. 权限系统 | 为多人协作预留接口 | 角色、权限、项目成员、资源所有权 |
| 2. 审计日志 | 操作可追溯 | 记录关键操作、AI生成内容、15天保留 |
| 3. 工作流引擎 | 自动化+实时状态 | 8步完整流程、流式更新、进度展示 |
| 4. 数据追溯 | 数据可信 | 来源标记、依赖链路、版本管理、血缘可视化 |
| 5. 指标体系 | 价值展示 | 准确率、完成量、工作量化、对外展示 |
| 6. 工作舱 | 统一工作界面 | 项目制、模块化、一站式工作 |
| 7. 资产库 | 经验复用 | Skill 系统、思考模式学习、动态更新 |

### 核心设计原则

1. **项目制思维**：所有工作围绕项目展开
2. **自动化优先**：能自动的就自动，减少人工干预
3. **可视化价值**：让用户看到系统创造的价值
4. **经验沉淀**：把成功经验固化为可复用资产
5. **AI学习用户**：从用户行为中学习，形成专属能力

### 实施优先级

#### 第一阶段（基础能力）
1. 权限系统 - 数据模型
2. 审计日志 - 数据模型
3. 工作流引擎 - 核心流程

#### 第二阶段（核心能力）
4. 数据追溯 - 血缘关系
5. 指标体系 - 价值展示
6. 工作舱 - 界面重构

#### 第三阶段（高级能力）
7. 资产库 - Skill 系统增强

---

## 实施计划

### 第一阶段：基础能力（模块1-2）
- [x] 模块1：权限系统 - 数据模型设计
- [ ] 模块1：权限系统 - 后端API实现
- [ ] 模块1：权限系统 - 前端集成
- [x] 模块2：审计日志 - 需求确认
- [ ] 模块2：审计日志 - 数据模型设计
- [ ] 模块2：审计日志 - 后端实现
- [ ] 模块2：审计日志 - 前端展示

### 第二阶段：核心能力（模块3-5）
待第一阶段完成后讨论

### 第三阶段：高级能力（模块6-7）
待第二阶段完成后讨论

### 最后阶段：Bug修复
- [ ] 解决照片列表显示问题
- [ ] 解决表格列表显示问题

---

## 附录

### 参考文档
- 《2026年企业AI工作流重构研究报告》
- FieldMind 现有架构分析

### 更新日志
- 2026-08-27: 创建文档，确认模块1、模块2设计
