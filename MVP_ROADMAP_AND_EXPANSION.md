# FieldMind MVP 跑通标准与扩展方案

**制定日期**: 2026-09-09  
**当前完成度**: 72%  
**MVP 目标**: 田野调查完整闭环

---

## 一、MVP 跑通的标准：什么算"跑通"？

### 核心目标
**一个完整的田野调研项目，从上传材料到产出决策报告，全程顺畅、无bug、用户能看懂。**

### 验收标准（6 条）

| 验收项 | 具体表现 | 当前状态 |
|--------|---------|---------|
| **1. 材料上传顺利** | 拖拽上传无报错，批量上传不丢文件，进度可感知 | ✅ 90% |
| **2. 转录与切分** | 音频转录完整，切分合理（200-500字/块），量化字段自动填充 | ✅ 85% |
| **3. 知识脉络可探索** | 大脉络聚类清晰，点击节点展开子脉络，关键词列表有频次 | ⚠️ 70% (后端完成，前端待做) |
| **4. 业态分析有依据** | 现有业态得分合理，可能业态推演逻辑可追溯，AI评估有材料依据 | ⚠️ 60% (需要完善) |
| **5. 三层报告可交付** | 事实层、洞察层、商业层生成完整，可下载为HTML/Markdown | ✅ 85% (后端完成) |
| **6. 数据可复盘** | 数据治理看板显示维度覆盖、缺口清单、可信度评分 | ❌ 30% (待实现) |

**做到这 6 条，MVP 就"跑通了"。** 不用完美，但每一条都要无报错地通。

---

## 二、当前缺什么功能板块？

### P0 优先级（必须补，否则跑不通）

#### 1. 协作与权限（最小版本）⭐⭐⭐
**为什么重要**：田野调研是团队活，一个人上传、另一个人分析、第三个人审阅。没有协作，每个项目只能一个人操作。

**怎么补**：
- 项目邀请制（生成链接，对方加入）
- 角色区分（管理员/编辑/只读）
- 操作记录（谁在什么时候做了什么）

**预计时间**: 4-6 小时

---

#### 2. 数据质量监控⭐⭐⭐
**为什么重要**：用户不知道"处理得对不对"。需要告诉他：哪些材料处理了、哪些处理错了、缺什么数据。

**怎么补**：
- 材料状态机（pending → processing → done → error）
- 每个材料显示处理状态
- 数据治理看板

**预计时间**: 3-4 小时

---

#### 3. 错误恢复与重试⭐⭐
**为什么重要**：转录失败、切分报错、量化算不出来 → 用户不知道怎么办。

**怎么补**：
- 每个步骤有"重试"按钮
- 错误日志可查看
- 失败原因显示（不要求用户懂技术，但要让用户能复制错误信息）

**预计时间**: 2-3 小时

---

#### 4. 溯源回溯（核心差异点）⭐⭐⭐
**为什么重要**：报告里一句话，用户不知道来自哪段原始材料。没有溯源，报告不敢用。

**怎么补**：
- 报告里每个结论句带"查看来源"按钮
- 点击弹出原始材料片段（音频时间码/文档原文）
- 溯源链：结论 → chunk → 原始文件

**预计时间**: 4-6 小时

---

### P1 优先级（体验提升）

#### 5. 知识脉络前端可视化⭐⭐
**当前状态**：后端已完成，前端待实现

**怎么补**：
- D3.js 力导向图
- 节点交互（点击、悬停）
- 侧边面板显示详情

**预计时间**: 4-5 小时

---

#### 6. 通用报告模板⭐
**为什么重要**：不同项目需要不同报告格式。

**怎么补**：
- 报告模板系统
- 用户可选择模板
- 支持自定义字段

**预计时间**: 3-4 小时

---

## 三、扩展留余地的具体做法

### 核心原则：引擎通用化，场景模板化

```
通用引擎层（不绑定场景）
    ↓
配置模板层（田野调查 / 公司大脑 / 其他）
    ↓
前端展示层
```

### 具体实现

#### 1. 实体模型通用化
```python
# ❌ 错误：绑定场景
class FieldWorkEntity:
    type: Literal["人物", "地点", "文化资产"]

# ✅ 正确：通用模型
class Entity:
    type: str  # 可配置
    category: str  # 可配置
    
# 配置文件
FIELD_WORK_CONFIG = {
    "entity_types": ["人物", "地点", "文化资产"],
    "dimensions": ["文化", "经济", "社会", "政策"]
}

COMPANY_BRAIN_CONFIG = {
    "entity_types": ["人员", "部门", "项目", "客户"],
    "dimensions": ["战略", "运营", "财务", "人力"]
}
```

#### 2. 工作流模板化
```python
# 工作流引擎（通用）
class WorkflowEngine:
    def execute(self, template: WorkflowTemplate):
        for step in template.steps:
            self.run_step(step)

# 田野调查模板
field_work_template = WorkflowTemplate(
    name="田野调查",
    steps=[
        {"name": "上传", "handler": "upload"},
        {"name": "转录", "handler": "transcribe"},
        {"name": "切分", "handler": "chunk"},
        {"name": "量化", "handler": "quantify"},
        {"name": "分析", "handler": "analyze"}
    ]
)

# 公司大脑模板（复用引擎）
company_brain_template = WorkflowTemplate(
    name="公司大脑",
    steps=[
        {"name": "采集", "handler": "collect"},  # 不同的 handler
        {"name": "清洗", "handler": "clean"},
        {"name": "切分", "handler": "chunk"},     # 复用
        {"name": "量化", "handler": "quantify"},  # 复用
        {"name": "分析", "handler": "analyze"}    # 复用
    ]
)
```

#### 3. Skill 系统可配置
```python
# 通用 Skill 框架
class Skill:
    name: str
    trigger_condition: Dict  # 可配置
    execution_logic: Callable  # 可配置
    
# 田野调查 Skill
fei_xiaotong_skill = Skill(
    name="费孝通框架分析",
    trigger_condition={"document_type": "field_work"},
    execution_logic=analyze_with_fei_framework
)

# 公司大脑 Skill（复用框架）
swot_skill = Skill(
    name="SWOT 分析",
    trigger_condition={"document_type": "business"},
    execution_logic=analyze_with_swot
)
```

#### 4. 外部数据接入预留
```python
# 数据源抽象
class DataSource(ABC):
    @abstractmethod
    def fetch(self) -> List[Document]:
        pass

# 文件上传（当前）
class FileUploadSource(DataSource):
    def fetch(self):
        return uploaded_files

# 邮件数据源（未来）
class EmailSource(DataSource):
    def fetch(self):
        return fetch_from_email_api()

# 数据库数据源（未来）
class DatabaseSource(DataSource):
    def fetch(self):
        return fetch_from_database()
```

---

## 四、接下来的执行顺序

### 第一周：MVP 跑通（P0）

#### Day 1-2: 数据质量监控
- [ ] 状态机实现
- [ ] 数据治理看板
- [ ] 错误恢复机制

#### Day 3-4: 溯源回溯
- [ ] 后端溯源 API
- [ ] 前端溯源面板
- [ ] 音频时间码定位

#### Day 5: 协作最小版本
- [ ] 项目成员管理
- [ ] 角色权限控制
- [ ] 操作日志

### 第二周：体验提升（P1）

#### Day 1-2: 知识脉络可视化
- [ ] D3.js 集成
- [ ] 交互逻辑
- [ ] 数据联动

#### Day 3-4: 报告模板系统
- [ ] 模板引擎
- [ ] 报告下载
- [ ] 自定义字段

#### Day 5: 测试与优化
- [ ] 完整流程测试
- [ ] 性能优化
- [ ] Bug 修复

---

## 五、扩展路径图

```
【当前：田野调查 MVP】
    ↓
【第一次扩展：田野调查完整版】
- 多人协作完善
- 高级分析功能
- 移动端支持
    ↓
【第二次扩展：公司大脑】
- 复用：核心引擎（切分、量化、图谱）
- 新增：业务数据源（CRM、ERP、邮件）
- 新增：商业 Skill（SWOT、波特五力）
- 新增：团队协作（OKR、项目管理）
    ↓
【第三次扩展：通用知识管理平台】
- 复用：所有核心能力
- 新增：行业模板库
- 新增：开放 API
```

---

## 六、关键决策点

### 什么时候开始扩展？

满足以下 3 个条件：
1. ✅ 田野调查 MVP 验收标准全部达成
2. ✅ 至少 10 个真实项目跑通（付费客户）
3. ✅ 用户反馈"核心功能稳定"

**不要提前扩展！** 先把田野调查做透，再横向复制。

### 如何验证"留余地"是否成功？

标准：
- 从田野调查切换到公司大脑，**核心代码改动 < 20%**
- 新场景只需要：配置文件 + 新 Skill + 新前端页面
- 核心引擎（切分、量化、图谱）完全复用

---

## 七、总结

### MVP 跑通 = 6 条验收标准全部通过

当前状态：
- ✅ 已完成：4 条（上传、转录切分、报告生成）
- ⚠️ 部分完成：2 条（知识脉络、业态分析）
- ❌ 待完成：4 个 P0 板块（协作、监控、重试、溯源）

### 预计完成时间

- P0 核心补齐：2 周
- P1 体验提升：1 周
- **MVP 完整跑通：3 周**

### 扩展留余地 = 引擎通用 + 场景配置

已实现：
- ✅ 核心引擎与场景解耦
- ✅ 配置驱动设计
- ✅ Skill 系统可扩展

---

**建议下一步**：立即开始 P0-1（数据质量监控），确保用户能看到"处理进度"和"错误状态"。

需要我立即开始实现吗？
